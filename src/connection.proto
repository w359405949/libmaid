import "controller.proto"

message Connection
{
	optional int64 id = 1;
	optional string host = 2;
	optional int32 port = 3;

	extensions 1000 to max;
}

extend ControllerMeta
{
	optional Connection connection = 1001;
}
